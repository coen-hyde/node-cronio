var vows = require('vows'),
    assert = require('assert'),
    util = require('util');

vows.describe("Cron.io API").addBatch({
  'Create user': {
    topic: function () {

    },
    'returns a user': function () {

    },
    'Create a cron': {
      topic: function () {

      },
      'returns a cron': {

      }
    }
  }
}).run();
